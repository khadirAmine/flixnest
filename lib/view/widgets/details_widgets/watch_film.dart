import 'package:flutter/material.dart';

class WatchFilm extends StatelessWidget {
  const WatchFilm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Watch Film'));
  }
}
