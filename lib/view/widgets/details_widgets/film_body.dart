import 'package:flutter/material.dart';

class FilmBody extends StatelessWidget {
  const FilmBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [
      Text('عروض مشابهة'),
    ]);
  }
}
